name: Test Build
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Chmod
      run: chmod +x gradlew
    - name: Test Gradle
      run: ./gradlew --version
    - name: Test Java
      run: java -version
